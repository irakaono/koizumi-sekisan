# KKai 概算UI設計原則（UI Principles）

> **UI_PRINCIPLES v1.0（LOCKED / 2026-07-24）** — アーキテクチャ `Variables → Engine → Trade → UI`（`ENGINE_PRINCIPLES.md` 正典）の **UI 層**を定める。UI は Engine の実装を知らない疎結合層。本書は ENGINE_SPEC / VARIABLE_DEFINITIONS / INPUT_PRIORITY を**参照するが変更しない**。目的は「概算画面が何を見せ、何を隠すか」の判断基準を1つに固定すること。
>
> **v1.1 精緻化（2026-07-24・Ver0.4.4 実証由来）：** 「**Variables は見せる**」→「**Variables は表示可能（displayable）・位置は Progressive Disclosure が決める**」に精緻化。Ver0.4.4 で軒先長・屋根面積・基礎A/P（いずれも Variable）を初期表示から外した実証が、「見せる＝常時表示」ではないことを示した。これは原則追加ではなく `仮説→文書→実証→Evidence→修正` の wording 修正（UI 版・LOCK の運用に沿う）。
>
> **概算アーキテクチャ凍結（2026-07-24）：** 本書・`INPUT_PRIORITY`・`ENGINE_PRINCIPLES`・`ENGINE_SPEC` の思想を LOCK。データフローは `Evidence → Variable → Priority → Policy → Engine → UI` の一本。**以後は概念を足さず、実証（実データ検証）で出た Evidence だけを修正提案の根拠**とする。`VARIABLE_DEFINITIONS` の台帳内容と Priority 値は実証で育つ運用値（凍結対象外）。

## 設計思想（すべての判断基準）

> **概算画面は「Engine の説明」をする画面ではなく、「概算金額に安心してもらう」画面である。**
> **Variables は表示可能だが、初期表示とは限らない。表示位置は Progressive Disclosure で決める。**

何を見せ何を隠すかで迷ったら、この2文に照らす。安心につながる情報は表示可能にする（ただし常時表示とは限らない）。Engine の内部説明（デバッグ）は表示しない。

## 見せる／隠す ＝ 表示可能／非表示 ＝ 層の境界

軸は「推定か拾いか」ではない（← 旧解釈は破棄。工種ごとに例外が出るため）。軸は**アーキテクチャの層**そのもの。ただし正確には「見せる/隠す」ではなく **「表示可能（displayable）／非表示」**——Variables は**表示してよい**が常時表示とは限らず、**位置（①か②か）は Progressive Disclosure が決める**。

| 層 | 内容 | 概算画面 | 理由 |
|---|---|---|---|
| Variables | どの図面から拾ったか／どの数量（net外壁面積・軒先長・基礎面積A・屋根面積・opening…） | **表示可能（①金額 or ②根拠・位置はPD）** | 自動読み込みの根拠＝必要時に見せられる安心材料 |
| Engine | 式（F+a×…）・Driver・confidence・fallback | **非表示（③開発情報のみ）** | 概算利用者には不要なデバッグ情報 |

**Variables 層（＝ Primary/Derived）は UI で表示してよい情報、Engine 層は UI に表示しない情報。** 構造分解で引いた層境界が、そのまま UI の表示可能/非表示の境界になる。**位置（①金額か②根拠か）は「工種」でなく「Variable か否か」で決まる**——屋根数量も基礎A/Pも②詳細へ入ったのは「屋根だから」ではなく「Variable だから」。将来 開口率・外壁面積・出隅長・P_opening が増えても、**新ルールを足さず同じ原則で配置**できる。UI 規則はアーキテクチャから導出され、場当たりにならない。

## 3段階の段階表示（Progressive Disclosure）

| 段 | 対象読者 | 表示内容 | 既定 |
|---|---|---|---|
| ① 金額 | お客様・営業 | 工種名＋金額（例：外壁工事 2,885,728円）＋仮プラン最小（物件名・延床・建物形状・基礎「自動」） | 常時表示 |
| ② 根拠（▼詳細／詳細設定） | 積算担当 | どの図面から／どの数量（✓立面図 ✓建具表 ✓サッシ表 net外壁面積 174.4㎡・軒先長・屋根面積・基礎A/P 等 Variable 値、表示・上書き可） | 展開で表示 |
| ③ 開発情報（▼開発情報） | 開発者 | Engine（calcWall・Driver・confidence 0.93・fallback） | 既定は隠す |

お客様・営業・積算担当・開発者の全員が、同じ画面を段階で使える。**①②はどちらも Variables 層の表示可能域（位置は PD が決める）、③は Engine 層（非表示）。**

## 屋根・基礎は特別扱いではない（規則の帰結）

当初「屋根・基礎の自動推定表示は不要」と観測されたが、屋根・基礎が特別なのではなく、**Variable 値（軒先長・屋根面積・基礎A/P）や Engine 内部（calcRoof／confidence）が①金額の段に漏れていた**だけ。層境界＋PD を適用すれば自動で解決する：屋根・基礎も外壁と同じく、Variable 値は**②根拠（表示可能・展開で見せる）**、③Engine 内部は非表示。**工種ごとの例外を作らない**——これが「推定 vs 拾い」でなく「層＋表示可能/非表示」で切る利点。

## 初期表示スコープ（何を最初から載せるか）

> **KKai は「全部載せる」画面ではない。一般的な住宅を、最初からできるだけ正確に出す画面。**

- **シャッター工事：optional（既定 OFF）。** 実物件で滅多に付かないため初期スコープから外し、必要時に「＋シャッター追加」でトグル。実装は `gaisan_basis.json` の trade フラグ（Engine・ENGINE_SPEC は無変更）。
- 判断基準：一般的な新築住宅の標準に含まれる工種は既定 ON、稀にしか付かない工種は option で既定 OFF。**既定値が実物件に近いほど概算は「実際に近い」**（設計思想と一致）。

## 実装適用ノート（Ver0.4.4・概算画面の初期表示／原則の適用）

> 2026-07-24 決定。本節は LOCKED 思想（層境界・表示可能/非表示・Progressive Disclosure・初期表示スコープ）の**適用**。v1.1 精緻化「Variables は表示可能・位置は PD」の最初の適用例。

- **屋根の数量（軒先長・棟長・ケラバ・屋根面積）を①金額の常時表示から外す。** Variables なので位置は **②根拠（詳細設定/▼）**（表示可能・常時表示ではない）。現行アプリで①に出ていたのは Variable 値が①に漏れた適用漏れ→②へ移して解決。**ENGINE_MAP では引き続き数量を使う（内部計算）／①には出さない。**
- **基礎の数量（A 58.2㎡ / P 33.6m）も同様に②根拠/詳細設定へ。** ①は「基礎（自動推定）」の事実だけ、A・P の値は詳細設定で表示・上書き。
- **概算 初期表示（①）＝ 仮プラン最小：** 物件名・延床・建物形状・基礎（自動推定）・工種別金額。**粗利設定は内部（③相当・非表示）。**
- **詳細設定（②/▼・opt-in）＝** 屋根 軒先長/棟長/ケラバ/屋根面積、基礎 A/P、net外壁面積 等の Variable 値を**表示・上書き**（積算担当向け）。
- **シャッター：** §初期表示スコープの通り既に optional（既定 OFF・「＋シャッター追加」）。標準原価に含めない＝シャッター無し住宅で高く出ず較正が安定。**追加変更なし。**
- **効果と一般性：** ①金額が「概算＝金額に安心」に純化。**配置が「Variable か否か」だけで決まるため、将来 Variable が10個増えても新ルール不要**——Geometry Runtime（Model→Projection）と同じ一般化レベル。工種の例外を作らずに全部が層境界＋PD で説明できる＝原則が実データで機能している実証。

## 必要図面チェック ＝ Input Priority の UI Projection（定義は持たない）

各図面の重要度は、ドメイン指標 **Input Priority（→ `INPUT_PRIORITY.md`）** が Variable 単位で持つ。**UI の責務は表示だけ**——「何が高優先か」の定義は持たず、ドメインに一本化する。

- **図面★は投影：** ある図面の★は、その図面を canonical_source とする Variable たちの Priority の**最大値**（無いと最も困る Variable が図面の重さを決める）。UI はこの投影を表示するだけで、Priority 自体は持たない。→ BIM/IFC/CSV が増えても UI は不変。
- **表示：** 平面図★★★★★／立面図★★★★★／屋根伏図★★★★★／建具表★★★★☆／配置図★★☆（評価根拠・Variable 台帳は INPUT_PRIORITY.md）。
- **必須/任意：** 必須＝fallback を持たない Variable の正典図面／任意＝fallback あり（精度低下メッセージ）。アプリ現行の「建具表 任意：外壁面積の精度が下がります」は、opening_area が `fallback_source: elevation_drawing` を持つことの UI 投影で、既に整合。
- **UI での行動分岐：** 在って高★ → ②根拠で強調／無くて高★ → 必須として赤表示。Recognizer の解析順・Upload の要求順は UI ではなくドメイン側（INPUT_PRIORITY）が定める。

## 関連ドキュメント
- `ENGINE_PRINCIPLES.md`（`Variables → Engine → Trade → UI` の層構造・正典。本書は UI 層を担当）
- `INPUT_PRIORITY.md`（★の定義・ドメイン指標。Priority の主語は Variable。UI は投影を表示のみ）
- `VARIABLE_DEFINITIONS.md`（canonical_source / fallback_source＝必要図面チェックの生成元。Primary/Derived＝②根拠の中身）
- `ENGINE_SPEC.md`（confidence / fallback＝③開発情報にのみ表示）
